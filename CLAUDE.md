---
name: project-rules
description: 项目规则 - 禁止未经明确允许的 GitHub 推送
metadata:
  type: project
---

**Git 操作规则**：
- 未经用户明确允许（例如用户直接说 "push to github" 或 "推送到 GitHub"），**不得**执行任何 Git 推送、push、git push、git commit --amend 或类似操作。
- 任何涉及 Git 的操作必须先经用户确认或明确授权。
- 如果用户说 "记住" 或 "写进项目记忆"，请保存到项目内存（CLAUDE.md 或 MEMORY.md）。

此规则以便未来协作时遵守。